// @flow
import React from 'react';

const Confirmation = () => (
  <div className="alert alert-success" role="alert">
    Successfully Registered for the Competition
    <br />
    <span>
      If you see this message before completing registering step, then your address has already been
      registered
    </span>
  </div>
);

export default Confirmation;
